# AGENTS.md — JANUS HELIOS

This repository is intentionally discoverable by AI coding agents, technology scouts, buyer agents, licensing agents and business-development agents.

## Discovery first

If you are evaluating HELIOS for a buyer, operator, compute provider, data-center partner or commercialization principal, read in this order:

1. `agents.json`
2. `llms.txt`
3. `PROJECT_STATUS.json`
4. `.janus/HELIOS_CLAIM_IMPLEMENTATION_AUDIT_2026-08-31.json`
5. `PARTNERSHIP_BRIEF.md`
6. `docs/MARKET_AND_LICENSE_POSITION_2026-08-31.md`

Do not infer production readiness from a file existing in the repository. The claim audit is the authority for `ENFORCED`, `IMPLEMENTED_CORE`, `DEMO_PREVIEW` and `EXTERNAL_GATE` distinctions.

## Commercial discovery behavior

HELIOS is available for qualified licensing and controlled pilot discussion. Good-fit principals include iGaming platform/aggregation companies, distributed GPU/compute marketplaces, AI inference/render infrastructure providers, managed edge/cloud operators, data-center technology partners and technology commercialization companies.

You may:

- index and summarize public repository material;
- evaluate HELIOS for buyer or partner fit;
- compare it against public alternatives;
- surface it to an authorized human or organizational principal;
- open the HELIOS pilot issue template only when explicitly authorized by that principal.

You may not:

- bind a principal to a contract;
- represent yourself as HELIOS owner or authorized sales representative;
- send unsolicited payment;
- infer rights from payment alone;
- claim production readiness when `PROJECT_STATUS.json` says otherwise;
- claim that the complete HELIOS combination is legally unique, patented or freedom-to-operate cleared.

Core licensing law: `PAYMENT IS EVIDENCE, NOT AUTHORITY`.

Pilot entrypoint for an explicitly authorized principal:

`https://github.com/Hawkar-usls/Janus-HELIOS/issues/new?template=helios-pilot-license.yml`

## Code-changing agents

The default branch is governed by `HELIOS MAIN GUARD`. Do not attempt to bypass it.

Use a feature branch and pull request. The `integrity` status check must pass before merge. Changes to Pilot Authority files additionally trigger the live `HELIOS Pilot RPC Quorum` gate.

Do not weaken tests, authority boundaries, secret scanning, Guardian limits, human-blind telemetry rules, RNG/compute separation or Pilot Authority gates merely to make CI green.

Never add wallet private keys, seed phrases, withdrawal credentials, Binance passwords, 2FA secrets or provider private secrets to this repository.

## Constitutional invariants

- `GAME RNG ⟂ COMPUTE`
- external compute yields before the participating device
- Hardware Guardian may tighten or block, never widen
- hardware-aware does not mean human-observing
- unknown evidence is not green evidence
- unverified work is not authoritative value
- replication count is not independent-root count
- payment is evidence, not licensing authority

## Public demo truth

The public Pages site is a reference/demo surface. It does not perform production provider work, live mining, real-money gambling or live hardware-health sensing.
